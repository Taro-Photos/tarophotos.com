import { ContactForm } from "@taro/design-system";

// Booking / inquiry form. `fallbackEmail` powers the "or email directly" link.
export const Default = () => <ContactForm fallbackEmail="hello@tarophotos.com" />;
