import { Prose } from "@taro/design-system";

// 長文を「つらつら」書くための塊 — 文字列を渡すと空行で段落に自動分割される。
// リンク・強調が要る段落だけ <P> を併用する。
export const AutoParagraphs = () => (
  <div style={{ padding: 24, maxWidth: 680 }}>
    <Prose>{`夜と朝のあいだには、はっきりした境目がない。空の色が少しずつ入れ替わっていくだけだ。

その入れ替わりの数分間が、いちばん長く感じられる。指先が冷たくなって、ファインダーの中だけが世界になる。

太陽は、地図の言うとおりの場所に来た。`}</Prose>
  </div>
);
